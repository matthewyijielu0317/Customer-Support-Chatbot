# Core API Keys (Required)
# Get these from the respective service providers.
OPENAI_API_KEY="your_openai_api_key_here"
PINECONE_API_KEY="your_pinecone_api_key_here"

# Application Environment
# Set to "dev" for development, "prod" for production.
ENVIRONMENT="dev"

# Database Connection Strings
# These are the default values for running the services via docker-compose.
# If you are running services locally outside of Docker, update these accordingly.
POSTGRES_DSN="postgresql+psycopg://user:password@localhost:5433/ecomm"
REDIS_URL="redis://localhost:6379/0"
MONGODB_URI="mongodb://localhost:27017"

# Frontend Configuration
# The base URL of the frontend application, used for CORS.
FRONTEND_BASE_URL="http://localhost:3000"

# Admin User Credentials
# Default credentials for the admin user.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSCODE="admin_passcode"

# Slack Integration (Optional)
# Fill these in to enable Slack notifications for agent escalations.
SLACK_WEBHOOK_URL=""
SLACK_BOT_TOKEN=""
SLACK_CHANNEL_ID=""